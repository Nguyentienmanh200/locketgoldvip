export default function DiscountsPage() {
  return (
    <div className="space-y-5">
      <div>
        <h1 className="text-xl font-semibold">Mã giảm giá & Giftcode</h1>
        <p className="text-sm text-[var(--text-secondary)] mt-0.5">Quản lý discount / giftcode</p>
      </div>
      <div className="card p-10 text-center text-sm text-[var(--text-muted)]">
        Đang hoàn thiện CRUD mã giảm giá và giftcode.
      </div>
    </div>
  );
}
